<%= form_for(@employee) do |f| %>
  <% if @employee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@employee.errors.count, "error") %> prohibited this employee from being saved:</h2>

      <ul>
      <% @employee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

<!--  <div class="field">  Pour choisir le departement en utilisant l'id du departement
  <p> <%= f.label :department_id %> ID</p>
  <br>
  <%= f.text_field :department_id %> 
  </div>
  <div class="actions">
-->

<p> Department</p> 
<!-- Liste deroulante où nous pouvont choisir le department auquel appartient un employé -->
<%= f.select :department_id, Department.all.collect { |p| [ p.name, p.id ] }, include_blank: true %>
<br><br>
 <%= f.submit %>
  </div>
<% end %>

